<p>Temporary placeholder for final registration form.</p>

<%= form_for @voter, url: url_for(controller: 'registrations', action: 'create') do |f| %>
  <div>
      <label>School</label>
      <%= f.select :school_id, options_for_select(@schools) %>
  </div>
  <div>
      <label>Gender</label>
      <%= f.select :gender, options_for_select([['Male', 'Male'], ['Female', 'Female']]) %>
  </div>
  <div>
      <label>Gender</label>
      <%= f.select :gender, options_for_select([['Male', 'Male'], ['Female', 'Female']]) %>
  </div>
  <div>
      <label>School Year</label>
      <%= f.select :school_year, options_for_select([['Freshman', 'Freshman'], ['Sophmore', 'Sophmore'], ['Junior', 'Junior'], ['Senior', 'Senior']]) %>
  </div>
  <div>
      <label>Registration Number</label>
      <%= f.text_field :registration_number %>
  </div>
  <div>
      <label>Will you be eighteen by election day?</label>
      <%= f.check_box :will_be_eighteen %>
  </div>
  <div>
      <%= f.submit 'Submit' %>
  </div>
<% end %>

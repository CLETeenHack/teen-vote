<%= form_for([:admin, @issue]) do |f| %>
  <% if @issue.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@issue.errors.count, "error") %> prohibited this admin_issue from being saved:</h2>

      <ul>
      <% @issue.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %><br>
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.label :description %><br>
    <%= f.text_area :description %>
  </div>
  <div class="field">
    <%= f.label :issue_type_id %><br>
    <%= f.number_field :issue_type_id %>
  </div>
  <div class="field">
    <%= f.label :voting_open_time %><br>
    <%= f.datetime_select :voting_open_time %>
  </div>
  <div class="field">
    <%= f.label :voting_closed_time %><br>
    <%= f.datetime_select :voting_closed_time %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
